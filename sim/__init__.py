"""
Simulacao do Drosobot.

Existe como pacote pra que `python -m sim.run_experiments` funcione. Os modulos
aqui dentro continuam sendo importaveis de forma plana (`import drosobot_lab`)
quando `sim/` esta no `sys.path` -- e o que os scripts de uma corrida so fazem.
"""
